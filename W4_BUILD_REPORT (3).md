# HYDRA_ARCHITECTURE_EVIDENCE_BRIDGE_V001

## W4 status

`W4_ACCEPTANCE=PASS`

W4 connects the recruiter proof strip and Constraint Case Study V2 to HYDRA's deeper technical architecture.

The section deliberately presents **two rails**:

- the architectural role of each layer;
- the evidence state that determines what may be claimed publicly.

## Architecture path

`SOURCE INTAKE`
→ `CONTROLLED REPRESENTATION`
→ `SOURCE AUTHORITY`
→ `CONTRACT SEAMS`
→ `LINEAGE + RELEASE GATES`
→ `CONSTRAINT SEMANTICS`
→ `ADMISSIBILITY`
→ `DOWNSTREAM CONSUMERS`

## Evidence states

`ARCHITECTURE` means the layer is part of the designed system, but W1 does not yet authorize execution language.

`REPRESENTATIVE` means the displayed public structure is illustrative.

`INTEGRATED` means current component/integration proof exists, without implying live data unless the receipt says so.

`SHADOW` means receipt-backed bounded shadow or synthetic-shadow execution.

`BLOCKED` means the system intentionally refuses progression because a required authority/contract seam is unresolved.

## Public wording enforced by W4

- Source intake remains architecture wording, not a verified multi-source ingestion claim.
- Controlled representation is not described as live end-to-end normalization.
- N4 source authority is shown as integrated and receipt-backed.
- Handoffs use the phrase `Contract-checked handoffs`.
- Lineage + release gates remain supported through CI-006.
- N1 semantic classification is shown as integrated `38/38 PASS`.
- T5→T6 admissibility remains visibly blocked.
- Downstream research/planning are shown as intended consumers, not a proven operational handoff.

## Site placement

Recommended order:

`RECRUITER HERO`
→ `RECRUITER PROOF STRIP`
→ `CONSTRAINT CASE STUDY V2`
→ `ARCHITECTURE + EVIDENCE BRIDGE`
→ deeper evidence / GitHub / implementation detail

## Hero rule

W4 does not alter the frozen recruiter hero.
