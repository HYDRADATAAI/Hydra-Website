# Website copy patches from W5

These are bounded claim-safety edits. They do not redesign the frozen hero.

## Hero badge

Replace:

`Multi-source ingestion`

with:

`Source-accounted ingestion`

Reason: the public receipt directly proves governed accounting across thousands of source files/records. It does not need the phrase “multi-source” to imply multiple independent providers.

Keep unchanged:

`Contract-bound handoffs` · `Lineage + release gates`

## Hero explainer

Use:

> HYDRA ingests market data, normalizes governed records, resolves identity, enforces contract-bound handoffs, preserves lineage and history, and exposes downstream intelligence only when the required authority is present.

This replaces wording that can be read as claiming live/integrated ingestion of every market/reference/history source family.

## W3 normalization sentence

Patched from a raw-preservation-sounding claim to:

> Source references keep each observation attributable instead of collapsing it into one anonymous fact.

## W3 interpretation heading

Patched to:

> This representative interpretation uses the narrowest conclusion supported by the stated evidence.
