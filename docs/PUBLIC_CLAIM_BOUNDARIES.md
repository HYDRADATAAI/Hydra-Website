# Public claim boundaries

The recruiter-facing market walkthrough is **REPRESENTATIVE** and **NON-LIVE**.

The public-safe CI-004 and CI-006 excerpts are **SYNTHETIC / SHADOW** control evidence.

The public-safe CI-008 authority example is **SHADOW** control evidence.

The public Python pipeline, its local checkpoint-recovery path, the SQLite data-quality sample, and the governed pre-model context/evaluation sample are **SYNTHETIC / NON-LIVE** engineering evidence.

The governed-intelligence sample keeps model execution disabled. It may demonstrate manifest integrity, bounded accepted context, exact citations, aggregate-only quarantine reporting, explicit `ADMIT` / `ABSTAIN` / `REFUSE` decisions, and deterministic evaluation receipts. It does not demonstrate model quality, semantic retrieval quality, natural-language intent classification, production inference, autonomous analysis, investment advice, or trading authorization.

The site does **not** claim:

- live production validation;
- canonical production promotion;
- production constraint detection;
- real-world market signal execution;
- a production database, data warehouse, or distributed query platform;
- production SLO attainment, uptime, incident-response history, or operating cost;
- a deployed orchestration or observability service;
- Baby ML training;
- Serious ML execution.

The public engineering claim is narrower: HYDRA contains tested mechanisms for contract-bound handoffs, lineage preservation, quarantine/release blocking, relational data-quality classification, checkpointed local recovery, integrity-checked replay, bounded accepted-context assembly, exact citation validation, explicit abstention/refusal, defect-scoped repair, deterministic validation, and fail-closed behavior when required authority is missing.


## External repository link

No GitHub URL is fabricated in this package. Bind only the real public repository URL before deployment.
